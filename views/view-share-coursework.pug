extends layout

mixin message(opp, name)
    if(opp)
        h3.greeting Welcome  
            span=name
    
        h4.greeting Share Coursework Project

block content
    include includes/header
    .dashboard 
        .student 
            +message(opp, name)